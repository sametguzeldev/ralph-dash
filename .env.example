# Path to your Ralph installation (mounted read-only)
RALPH_PATH=/ralph-path

# Path to your projects directory (mounted read-write for file sync)
# All projects you add to the dashboard must be under this path
WORKSPACE=/workspace
