# Ralph Progress Log
Started: Fri May  8 11:08:52 UTC 2026
---

## 2026-05-08 11:10 - Parallel wave: US-001,US-002
- Stories ran in parallel worktrees via `claude --worktree`
---

## 2026-05-08 11:12 - Parallel wave: US-003,US-004
- Stories ran in parallel worktrees via `claude --worktree`
---

## 2026-05-08 11:16 - Parallel wave: US-005,US-006,US-007
- Stories ran in parallel worktrees via `claude --worktree`
---

## 2026-05-08 11:21 - Parallel wave: US-008,US-009
- Stories ran in parallel worktrees via `claude --worktree`
---

## 2026-05-08 - US-010
- Added post-review action buttons (Done and Fix & Re-run) to ReviewStep
- Done button calls archiveProject API, invalidates workflow/project queries, shows success message
- Fix & Re-run button calls saveReviewFeedback API, then navigates wizard to Run step (step 4)
- Both buttons disabled while review is still running via `reviewDone` derived state
- Added `saveReviewFeedback` and `archiveProject` API functions to api.ts
- Files changed: frontend/src/components/WorkflowWizard.tsx, frontend/src/lib/api.ts
- **Learnings for future iterations:**
  - ReviewStep uses `onGoToRun` callback from parent to navigate; parent calls `setActiveStep(3)` + `setUserOverride(true)` to force step change
  - `reviewDone` is derived from `hasStarted && !isReviewRunning && status in (completed, failed)` to only show action buttons after a real review run
  - The `queryClient.invalidateQueries` pattern is used to refresh workflow state after archive resets files
---

## 2026-05-08 - US-011
- Added "Skip & Archive" button to ReviewStep, always visible when prd.json is valid and review isn't running
- Clicking shows inline confirmation: "Archive this feature without reviewing?" with Yes/Cancel buttons
- On confirmation, runs the same archiveProject flow as the Done button (archive + invalidate queries + success message)
- Added `showSkipConfirm` state and `handleSkipArchive` handler
- Files changed: frontend/src/components/WorkflowWizard.tsx
- **Learnings for future iterations:**
  - Inline confirmation patterns (show/hide with state toggle) work well for destructive actions instead of browser confirm() dialogs
  - The `&amp;` entity is needed for `&` in JSX text content (used for "Skip & Archive" button label)
  - Gating UI elements on `hasPrdJson && prdJsonValid` prevents actions on invalid state without needing the review provider
---
